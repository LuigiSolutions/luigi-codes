# Build output & caches
out/
node_modules/
.vscode-test/
*.vsix

# Session-local files — machine-specific handoff notes stay off GitHub
HANDOFF.md
.claude/

# Internal strategy docs — keep off the public repo (local reference only)
docs/

# Eval run artifacts (harness + task sets are tracked; per-run reports are not)
eval/reports/

# OS noise
.DS_Store
.vercel
.env*
